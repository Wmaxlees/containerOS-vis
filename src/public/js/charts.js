var Charts = {
  updateCPU,
  updateMem
}

// highcharts chart object declaration
// highcharts other thingie object declaration

function updateCPU (a/* the array [Date.now(),x1,x2,..,xn]*/) {
  console.log('updateCPU called with ' + a)
  // updates chart

}

function updateMem (a/* whatever you need here too */) {
  console.log('updateMem called with ' + a)
  // updates other thingie

}